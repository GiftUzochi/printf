#include main.h
#include <stdio.h>

/**
* print_field_width - function to specify the field width as an arguement
*@element: the va_list element passed from printf.c
*Return: the number of characters
*/
int print_field_width(va_list element)
{
va_start(args, value);
int i = 0, j = 1;
for (int 1 = 0)
_putchar('0')
